#include "main.h"
/**
 *
 *
 *
 */
int _strcmp(char *s1, char *s2)
{
	int i, a1, a2;

	for (i = 0; i++;)
	
	{	
		if (s1[i] == '\0' && s2[i] == '\0')
		{
			_putchar (0);
		}
		 a1 = (int) s1[i];
		 a2 = (int) s2[i];
		if (a1 > a2)
			_putchar (1);
		if (a2 > a1)
			_putchar (-1);
	}
	return (0);
}
