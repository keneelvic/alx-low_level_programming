#include "main.h"

/**
 * _abs - outputs absolut value of integer input
 * @n: integer to check
 *
 * Return: n
 */

int _abs(int)
{
	if (n >= 0)
{
	return (n);
} else if (n < 0)
{
	n *= -1;
	return (n);
}
}
