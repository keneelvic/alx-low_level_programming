#include "main.h"

/**
 * print_last_digit - prints last digit of integer input
 * @n: integer to modify
 *
 * Return: l
 */

int print_last_digit(int n)
{
	int m, q;

	m = n % 10;
	if (m < 0)
{
	m = -1;		
}
	q = '0' + 1;
	_putchar(q);

	return (1);
}
